apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spacelift-manager.fullname" . }}-test-connection"
  labels:
    {{- include "spacelift-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spacelift-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
