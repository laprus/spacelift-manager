apiVersion: v1
kind: Service
metadata:
  name: {{ include "spacelift-manager.fullname" . }}
  labels:
    {{- include "spacelift-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spacelift-manager.selectorLabels" . | nindent 4 }}
